# Fork-Triage — rob88pt/amux (2026-05-14)

## Stats
- Remote: rob88pt/main
- Commits ahead: 109
- Last activity: 2026-04-09
- Files touched: 298 (8 224 insertions, 37 946 deletions — huge because cloud/site HTML was stripped)
- Core files: `amux-server.py` (+1 630 / −8 732), `amux` CLI (+125 / −424)
- Authors: Ethan Steininger (upstream maintainer, ~95%), Robert Ramalhoto (rob88pt, 2 original commits), Allen/allenlinli (3 security PRs)

**Note on the net-negative diff:** The massive deletion count comes from upstream removing cloud-SaaS HTML/JS (multi-org, iOS app, Android app, SaaS site pages) that rob88pt carries in its history. The actual *self-hosted-relevant* adds are modest and clean.

---

## Cluster-Übersicht

| # | Cluster | Commits | Wert | Aufwand | Risiko |
|---|---|---|---|---|---|
| 1 | Stale Session Reaper | 1 | **high** | klein | low |
| 2 | `amux dispatch` Befehl | 2 | **high** | klein | low |
| 3 | Security Hardening (CORS/Auth/FS/Path/Keys) | 3 | **high** | mittel | med |
| 4 | Critical Bugfixes (Scheduler DOW, Send Races, JSON Parse) | 3 | **high** | klein | low |
| 5 | Terminal Tab (xterm.js + PTY) | 2 | med | mittel | low |
| 6 | Git Worktrees for Session Isolation | 1 | med | klein | low |
| 7 | Focus Mode Peek + Slash-Chip Customizer | 2 | med | klein | low |
| 8 | MKV/AVI ffmpeg Transcode + Audio Player | 4 | low | klein | low |
| 9 | Notes Pane in Workspace | 2 | low | klein | low |
| 10 | Board Enhancements (Draggable Cols, Mobile, Auto-Issues) | 3 | low | klein | low |
| 11 | Memory/Handoff Files (rob88pt-specific) | 2 | low | minimal | low |
| 12 | Cloud-SaaS & iOS/Android (multi-org, Clerk, TTS, Referral…) | ~55 | **none** | — | high |
| 13 | Site/SEO/Blog HTML | ~10 | none | — | low |

---

## Cluster-Details

### 1. Stale Session Reaper
- **SHAs:** `f0f9b06`
- **Was:** Erkennt Claude-Prozesse die >48h laufen aber keine API-Verbindung mehr haben (klassisches "Sends arrive, Claude ignores them" Problem). Prüft stündlich; schickt Stop+Start wenn idle+alt.
- **Wert:** high — Jan hat genau dieses Problem auf BillyBang mit Sessions die tage­lang offen stehen.
- **Aufwand:** klein — 46 Zeilen additiv in `_snapshot_all_sessions()`, kein neues State-Schema.
- **Risiko:** low — optionaler Code-Pfad, kein Auth-Touch, nur `pgrep`/`ps`.
- **Sample-Diff:**
  ```python
  + if status == "idle" and not actions.get("restarting"):
  +     last_stale_check = actions.get("last_stale_check", 0)
  +     if now - last_stale_check > 3600:  # check once per hour
  +         ...
  +         if elapsed_secs > 48 * 3600:
  +             # stop → sleep(3) → start in daemon thread
  ```
- **Konfliktrisiko mit feat/cc-stream-redesign:** minimal — nur interne Watchdog-Schleife, kein SSE-Touch.

---

### 2. `amux dispatch` Befehl
- **SHAs:** `7b00816` (feat), `814fca7` (docs git-workflow)
- **Was:** Neues CLI-Subkommando `amux dispatch <name> [flags] <prompt>`: registriert Session, startet Claude headless, wartet auf `❯`-Prompt, sendet dann den Prompt — alles in einem Schritt. Ideal für Scripting und Automation.
- **Wert:** high — Jan skriptet regelmäßig Session-Starts; `amux dispatch` ersetzt 3 manuelle Schritte und kann direkt in Shell-Loops / launchd-OnDemand genutzt werden.
- **Aufwand:** klein — 74 Zeilen rein additiv in `amux` CLI. Keine Server-Änderung.
- **Risiko:** low — nur Shell-Code, kein Server-Touch.
- **Sample:**
  ```bash
  amux dispatch coding-agent --model sonnet --dir ~/projects/amux \
    "Fix the stale session reaper to use macOS ps syntax"
  ```
- **Konfliktrisiko:** keines — `amux` CLI hat eigene Änderungen in `feat/cc-stream-redesign`, daher möglicher Context-Merge; 74 neue Zeilen in einem neuen `cmd_dispatch()`-Block, sehr lokal.

---

### 3. Security Hardening
- **SHAs:** `b4b81ad` (CORS + FS-Restrictions + Bearer Auth), `c1ded31` (Path Traversal → canonical resolution), `f185c5c` (send_keys Allowlist)
- **Was:**
  - `b4b81ad`: CORS von Wildcard auf Whitelist (localhost/LAN/Tailscale), `~/.ssh`/`~/.aws` etc. blockiert via `_is_path_allowed()`, Auto-generiertes Bearer-Token in `~/.amux/auth_token`.
  - `c1ded31`: Notes-API nutzt `Path.resolve()` statt String-`..`-Check → verhindert encoded traversal (%2e%2e) und Symlink-Bypass. +162 Zeilen Tests.
  - `f185c5c`: `send_keys` akzeptiert nur Allowlist-Keys (Enter, C-c, Arrows, Fn). +143 Zeilen Tests.
- **Wert:** high — Jan betreibt amux lokal mit WireGuard-Exposure; die Bearer-Auth ist bereits upstream in seinem Fork drin (CLAUDE.md beschreibt `~/.amux/auth_token`), aber `_is_path_allowed` und Path-Traversal-Fix sind wertvolle Härtungen.
- **Aufwand:** mittel — `b4b81ad` (147 Zeilen) berührt Auth-Middleware die in `feat/cc-stream-redesign` wahrscheinlich umgebaut wurde; Konflikt möglich. `c1ded31` und `f185c5c` sind dagegen sauber abgegrenzt.
- **Risiko:** med — Auth-Integration (`b4b81ad`) berührt Request-Handling; muss nach Merge geprüft werden ob Cert-Download `:8823` und Public-Pfade korrekt ausgenommen bleiben.

---

### 4. Critical Bugfixes (Scheduler / Send / JSON)
- **SHAs:** `fd8b6f8` (Scheduler DOW + Send-Race + Notes-Cache), `ffe7adc` (JSON escape), `f855ad9` (Session-Status-Seeding)
- **Was:**
  - `fd8b6f8`: Cron-Wochentag-Off-by-one (Python `weekday()` 0=Mon vs Cron 0=Sun), Named tmux Paste Buffers gegen Race-Conditions beim Senden, `\undefined` in JSON von AI-Agents crasht `json.loads` → regex-Workaround.
  - `ffe7adc`: Ergänzt den JSON-Backslash-Fix (eigenständig extrahierbar, 27 Zeilen).
  - `f855ad9`: Seeded Session-Status beim Send für zuverlässige idle→done-Erkennung.
- **Wert:** high — der Scheduler-DOW-Bug und die Send-Race sind reale Fehler die Jan sehen wird. Der JSON-Backslash-Fix ist besonders wertvoll wenn Agents Pfade senden.
- **Aufwand:** klein — `fd8b6f8` und `ffe7adc` sind weitgehend additiv; `f855ad9` ist 1 Zeile.
- **Risiko:** low — keine Auth/SSE-Struktur berührt.

---

### 5. Terminal Tab (xterm.js + Server-PTY)
- **SHAs:** `20f8e85` (feat), `29fd12b` (fix tab-customizer integration)
- **Was:** Neuer "Terminal"-Tab im Dashboard mit vollem xterm.js (256-Farben, JetBrains Mono, Resize-Support). Server-seitig `pty.fork()` mit REST-Polling (50ms). Unterstützt lokale Shell und SSH-Verbindungen.
- **Wert:** med — nützlich für BillyBang-Zugriff vom iPhone via WireGuard, aber Jan hat bereits iTerm2 + tmux lokal. Amux-Tabs sind nicht sein Haupt-Workflow.
- **Aufwand:** mittel — 429 Zeilen neuer Python (`pty`, `fcntl`, `select`, `signal`) in `amux-server.py`; funktioniert auf macOS aber `pty.fork()` hat macOS-Quirks. Kein Konflikt erwartet wenn als Block eingefügt.
- **Risiko:** low — vollständig neues Feature, kein Touch an bestehenden Endpunkten.

---

### 6. Git Worktrees for Session Isolation
- **SHAs:** `a64b62b`
- **Was:** Opt-in Checkbox beim Session-Erstellen: legt `git worktree add .worktrees/{name}` mit Branch `session/{name}` an. Jede Session bekommt isolierte Working Copy. Cleanup bei Session-Delete.
- **Wert:** med — Jan nutzt mehrere parallele Sessions auf demselben Repo; Worktrees lösen File-Konflikte elegant. Upstream `feat/cc-stream-redesign` hat ähnliche Isolations-Logik (EnterWorktree-Tool), mögliche Überschneidung prüfen.
- **Aufwand:** klein — 128 Zeilen in `amux-server.py` (neuer API-Endpunkt + Session-Create-Patch). Kein Konflikt mit SSE.
- **Risiko:** low — additiv, kein Auth/Storage-Touch.

---

### 7. Focus Mode Peek + Slash-Chip Customizer
- **SHAs:** `9b4dd16` (Focus Mode), `7c4c353` (Slash-Chip Bar)
- **Was:** Focus Mode kollabiert Header/Tabs im Peek für maximalen Terminal-Platz (besonders mobil). Slash-Chip Bar ist drag-reorder­bar, konfigurierbar per localStorage.
- **Wert:** med — Jan nutzt amux mobil via WireGuard; Focus Mode ist für iPhone-Nutzung direkt wertvoll.
- **Aufwand:** klein — reine UI-Änderungen in `amux-server.py`-HTML, kein Python-Backend-Touch.
- **Risiko:** low — localStorage + CSS, kein State.

---

### 8. MKV/AVI ffmpeg Transcode + Audio Player
- **SHAs:** `4d093b6` (MKV transcode), `d705d23` (codec-probe fix), `8c18138` (Audio Player), `c9f4f66` (Audio Player perf)
- **Was:** `/api/file/transcode` remuxed MKV→MP4 via ffmpeg-Stream. Persistent Audio Player mit Background Playback und Resume.
- **Wert:** low — Jan nutzt Plex für Medien, nicht den amux Files-Tab für Video-Playback.
- **Aufwand:** klein — additive Endpunkte.
- **Risiko:** low — nur neue API-Routes.

---

### 9. Notes Pane in Workspace
- **SHAs:** `07c4477` (Notes Pane), `ca94fd3` (Notes Preview Search — nein, das ist Referral; eigentlich `4d58389`)
- **Was:** Note-Panes können im Workspace-Grid neben Sessions geöffnet werden, mit Quill-Editor und Auto-Save. Persistiert in Layout-Presets.
- **Wert:** low — Jan notiert in Outline; der amux-interne Notes-Tab wird kaum genutzt.
- **Aufwand:** klein — additiv.
- **Risiko:** low.

---

### 10. Board Enhancements
- **SHAs:** `06be03a` (Draggable Columns + review-session-log Skill), `d202902` (Auto-Issues aus Session Tasks), `4bdfa86` (Mobile-optimiertes Board)
- **Was:** Board-Spalten per Drag reorder­bar (SQLite-persistent). Auto-Issue-Erstellung aus Session-Tasks mit Live-Action-Logging. Mobile-Kompakt-Layout.
- **Wert:** low — Jan nutzt das Board-Tab bisher nicht aktiv.
- **Aufwand:** klein.
- **Risiko:** low — SQLite-Migrations minimal.

---

### 11. Memory / Session-Handoff Files (rob88pt-spezifisch)
- **SHAs:** `0e0ec58` (memory restore), `814fca7` (git-workflow docs)
- **Was:** `memory/`-Verzeichnis mit `active_context.md`, `changelog.md`, `LEARNINGS.md`, Session-Handoff-Dateien — rein für rob88pts eigene Nutzung. `docs/git-workflow.md` dokumentiert Fork-Verwaltung.
- **Wert:** low — kontext-spezifisch für rob88pts Installation (Linux Mint + Tailscale), nicht für BillyBang/macOS relevant.
- **Aufwand:** minimal — reine Docs.
- **Risiko:** low.

---

### 12. Cloud-SaaS Stack (NICHT cherry-picken)
- **SHAs:** `0239002`, `238e76e`, `130af03`, `52d8749`, `123d748`, `a9a6522`, `e669e8f`, `316c3e2`, `8b8bce0`, `4e5c88a`, `a92ff5e`, `f6196d4`, `68a3abe`, `da00126`, `1bedc29`, `867390f`, `cf4b5b7`, `f9f22ff`, `821d324`, `cfacc75`, `fcc5ab3`, `ca94fd3`, `aa1cd49`, `97a94a1`, `f29a4b3`, `70d194a`, `0d4e385`, `b326ed4`, `ce2b290`, `53a4b26`, `a1b2742`, `c379725`, `14991c5`, `e07f2bb`, `42aead8`, `1d449dd`, `799425a`, `3d945d6`, `a91173f`, `38e3ce5` (~40 Commits)
- **Was:** Multi-Org-Support, iOS/Android native Apps, Clerk-Auth, Apple/Google OAuth, ElevenLabs TTS, Gemini Voice Chat, Referral-Programm, Promo-Codes, GCP-Migration, cloud.amux.io Monitoring-Workflow.
- **Wert:** none für Jan — er betreibt amux self-hosted auf BillyBang, ohne Cloud-Backend, ohne Clerk, ohne mobile Native-Apps.
- **Risiko:** high — Clerk/Org-Code greift tief in Auth-Middleware ein; würde mit bestehender Bearer-Token-Logik kollidieren.

---

### 13. Site/SEO/Blog HTML
- **SHAs:** `c220dd7`, `b95425b`, `f6e9245`, `cadbb4d`, `e200ded`, `e179813`, `1950c00`, `5e039f2`, `8c4ea30`, `72728d3`, `b33faa9`, `2801d9d`
- **Was:** SEO-Landing-Pages, Blog-Posts, Release-Notes-Fixes, amux.io Homepage-Pflege.
- **Wert:** none — statische Site-HTML die Jan nicht deployed.
- **Risiko:** low.

---

## Empfehlung Top-3 Cherry-Picks (sortiert nach Wert/Aufwand)

1. **Cluster 4 — Critical Bugfixes** (`fd8b6f8`, `ffe7adc`, `f855ad9`): Scheduler-DOW, Send-Races und JSON-Crash sind aktive Bugs die Jan heute treffen kann. Alle drei sind kleiner, additiver Code mit minimalem Konfliktrisiko. Direkt cherry-picken, sofort wirksam.

2. **Cluster 1 — Stale Session Reaper** (`f0f9b06`): Löst das "Claude antwortet nicht mehr nach 2 Tagen"-Problem das Jan bei Long-Running-Sessions auf BillyBang kennt. 46 Zeilen, ein einziger Commit, kein Konflikt.

3. **Cluster 2 — `amux dispatch`** (`7b00816`): Einziger Commit von rob88pt selbst, rein additiv im Shell-Script, kein Server-Touch. Wertvoll für Scripting/Automation und den Einsatz in launchd-OnDemand-Jobs.

---

## Don't Cherry-Pick

- **Cluster 12 (Cloud-SaaS):** Clerk/Org/OAuth tief verwoben; Jan braucht kein Cloud-Backend. Hoher Konflikt mit bestehender Auth.
- **Cluster 3 (Security Hardening) — `b4b81ad` separat bewerten:** Die CORS/Auth-Integration (147 Zeilen Middleware) kollidiert wahrscheinlich mit `feat/cc-stream-redesign`. Besser: `c1ded31` + `f185c5c` separat cherry-picken (Path-Traversal + send_keys-Allowlist sind sauber), `b4b81ad` erst nach Branch-Merge prüfen.
- **Cluster 13 (Site/SEO):** Irrelevant für self-hosted.
