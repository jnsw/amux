# Fork-Triage — j3rm/amux (2026-05-14)

## Stats
- Remote: j3rm/main
- Commits ahead: 7 (alle 2026-04-30 – 2026-05-03)
- Last activity: 2026-05-03

---

## Commits

### 1. 546a02f — 2026-04-30 — feat: Pushover push notifications for session alerts
- **Diff:** 1 file, +126/-1 (`amux-server.py`)
- **Was:** AMUX_PUSHOVER_TOKEN + AMUX_PUSHOVER_USER in server.env; Push bei Alert, "waiting", /api/notifications. Settings-Panel mit App-Token/User-Key-Fields + "Send test"-Button.
- **Schon bei Jan?** **Ja** — `31bb92a feat: Pushover push notifications for session alerts` + `7312b5d feat(redesign): expose Pushover settings in redesign Settings tab`
- **Wert:** n/a
- **Aufwand:** n/a
- **Risiko:** n/a
- **Sample:**
  ```diff
  +AMUX_PUSHOVER_TOKEN / AMUX_PUSHOVER_USER → _push_alert() fires on any alert,
  +"waiting" state, or POST /api/notifications. /api/pushover/test endpoint.
  ```

---

### 2. 8090d17 — 2026-04-30 — feat: inbound webhooks — SMS Eagle and SmarterTrack
- **Diff:** 1 file, +74/-1 (`amux-server.py`)
- **Was:** Zwei unauthenticated Webhook-Endpoints: `POST /api/webhooks/sms/<session>` (SMS Eagle → injiziert "SMS from X: text" in Session), `GET|POST /api/webhooks/smartertrack/<session>` (SmarterTrack new-chat → formatierte Summary). Beide in `_PUBLIC_PREFIXES`.
- **Schon bei Jan?** **Ja** — `8505284 feat: inbound webhooks — SMS Eagle and SmarterTrack`
- **Wert:** n/a
- **Aufwand:** n/a
- **Risiko:** n/a
- **Sample:**
  ```diff
  +POST /api/webhooks/sms/<session>  # SMS Eagle inbound
  +GET|POST /api/webhooks/smartertrack/<session>  # SmarterTrack new-chat
  +Both added to _PUBLIC_PREFIXES (no auth required)
  ```

---

### 3. e336a28 — 2026-05-03 — fix: prevent watchdog kill loop from stale tmux scrollback
- **Diff:** 1 file, +12/-4 (`amux-server.py`)
- **Was:** Reactive Watchdog-Handler (thinking-block corruption, session-ID conflict) matchten Fehlermeldungen aus altem tmux-Scrollback → Infinite Kill-Loop nach Restart. Fix: `clean_recent = last 200 lines` für reactive Handler (statt 5000), plus `tmux clear-history` vor hard-kill+restart.
- **Schon bei Jan?** **Nein** — `clean_recent` fehlt komplett in `feat/cc-stream-redesign`. Jan hat `tmux clear-history` nur für den `/api/.../clear`-Action-Endpoint, nicht im Watchdog.
- **Wert:** HIGH — stoppt echten production Infinite-Loop-Bug
- **Aufwand:** LOW — 16 Zeilen, chirurgisch
- **Risiko:** LOW — context-percentage-Detection behält absichtlich die 5000-Zeilen-Variante
- **Sample:**
  ```diff
  +clean_recent = "\n".join(clean.splitlines()[-200:])
   # reactive handlers now use clean_recent instead of clean:
  -if ("redacted_thinking" in clean and "cannot be modified" in clean ...):
  +if ("redacted_thinking" in clean_recent and "cannot be modified" in clean_recent ...):
  +    subprocess.run(["tmux", "clear-history", "-t", tmux_target(name)], ...)
  ```

---

### 4. 2a3d755 — 2026-05-03 — fix: clear tmux scrollback on session start to prevent watchdog false-fires
- **Diff:** 1 file, +5/-1 (`amux-server.py`)
- **Was:** Ergänzung zu Commit 3: `start_session()` ruft bei Wiederverwendung einer bestehenden tmux-Session sofort `tmux clear-history` auf, bevor Claude gestartet wird. Verhindert false-fires bei Server-Restart (wenn `_session_auto_actions`-Cooldowns resettet sind).
- **Schon bei Jan?** **Nein** — `start_session()` in `feat/cc-stream-redesign` hat kein pre-launch `clear-history`.
- **Wert:** HIGH — vervollständigt den 2-Layer-Fix aus Commit 3, ohne den geht 3 nur halb
- **Aufwand:** MINIMAL — 5 Zeilen
- **Risiko:** NONE — clear-history auf Session-Start ist idempotent
- **Sample:**
  ```diff
  -# Existing tmux session -- reuse it
  +# Existing tmux session -- reuse it, but wipe scrollback first so
  +# stale error strings don't trigger the watchdog on the new Claude process.
  +subprocess.run(["tmux", "clear-history", "-t", tmux_target(name)], ...)
  ```

---

### 5. 8567ffa — 2026-05-03 — fix: cherry-pick upstream OOM/resume-picker fixes (0a7b8b1, 562a58f)
- **Diff:** 1 file, +73/-3 (`amux-server.py`)
- **Was:** j3rm cherry-pickt upstream-Commits 0a7b8b1 (stuck --resume picker nach OOM-Kill) + 562a58f (auto-hibernate idle >30 min, auto-wake on send). Jan hat beide bereits direkt von upstream (`0a7b8b1`, `562a58f` sind in `feat/cc-stream-redesign`).
- **Schon bei Jan?** **Ja** — `0a7b8b1 Detect and recover from stuck --resume picker after OOM kill` + `562a58f Add auto-hibernate for idle sessions + auto-wake on send` direkt im Branch.
- **Wert:** n/a
- **Aufwand:** n/a
- **Risiko:** n/a
- **Sample:**
  ```diff
  +def _at_resume_picker(clean_output):  # from upstream 0a7b8b1
  +    return bool(... "Resume Session" ... "⌕" in clean_output)
  +# auto-hibernate logic from 562a58f (idle >30min → stop, auto-wake on send)
  ```

---

### 6. bea3956 — 2026-05-03 — fix: robust resume-picker detection + watchdog escape
- **Diff:** 1 file, +27/-5 (`amux-server.py`)
- **Was:** Zwei Fixes: (1) `_at_resume_picker()` prüft nicht mehr auf `⌕` Unicode (tmux capture-pane dropped es inkonsistent) — stattdessen "Resume Session" + beliebiger secondary signal. (2) Watchdog-Sektion 1c erkennt stuck picker der `start_session()` verpasst hat und schickt Escape+Ctrl-C.
- **Schon bei Jan?** **Teilweise** — Jan hat die alte `_at_resume_picker()`-Implementierung mit `⌕`-Check (Zeile 1586-1591 in `feat/cc-stream-redesign`). Der ⌕-Bug + der robustere secondary-signal-Check fehlen. Watchdog-Sektion 1c fehlt ebenfalls.
- **Wert:** HIGH — der ⌕-Bug macht die gesamte picker-Detection unzuverlässig
- **Aufwand:** LOW — 32 Zeilen gesamt
- **Risiko:** LOW — ⌕-Entfernung ist strictly safer
- **Sample:**
  ```diff
  -    return bool(clean_output and ... and "⌕" in clean_output)
  +    if "Resume Session" not in clean_output: return False
  +    return any(s in clean_output for s in ("Type to Search", "Esc to cancel", "1 of ", ...))
  +# watchdog 1c:
  +if _at_resume_picker(clean_recent) and now - actions.get("last_picker_escape", 0) > 30:
  +    tmux send-keys Escape; tmux send-keys C-c; _push_alert(...)
  ```

---

### 7. b8069da — 2026-05-03 — fix: accept resume picker with Enter instead of Escape+Ctrl-C
- **Diff:** 1 file, +18/-29 (`amux-server.py`)
- **Was:** Escape+Ctrl-C exitete Claude → auto-restart → `--resume` → picker → loop. Fix: Enter drückt den top-Eintrag im Picker, Claude resumed korrekt. Cooldown 30s → 60s. Gilt in `start_session()` und Watchdog.
- **Schon bei Jan?** **Nein** — `start_session()` in `feat/cc-stream-redesign` nutzt noch Escape+Ctrl-C (Zeile 5926-5930). Das ist der Loop-auslösende Bug.
- **Wert:** CRITICAL — Escape+Ctrl-C-Strategie ist aktiv defekt (erzeugt Loop)
- **Aufwand:** LOW — Net -11 Zeilen (vereinfacht)
- **Risiko:** LOW — Enter auf top-Eintrag ist das korrektere Verhalten
- **Sample:**
  ```diff
  -    subprocess.run(["tmux", "send-keys", ..., "Escape"], ...)
  -    subprocess.run(["tmux", "send-keys", ..., "C-c"], ...)
  +    subprocess.run(["tmux", "send-keys", ..., "Enter"], ...)
  +    # wait up to 10s for Claude UI (was: wait for shell prompt)
  +    for _w in range(20): ...
  ```

---

## Empfehlung

**Sofort cherry-picken (als Einheit, Reihenfolge beachten):**
- **e336a28** — watchdog clean_recent + clear-history bei kill
- **2a3d755** — clear-history in start_session() (Voraussetzung für 3+6+7)
- **bea3956** — robuste picker-detection ohne ⌕
- **b8069da** — Enter statt Escape+Ctrl-C (CRITICAL: fixt aktiven Loop-Bug)

**Skip:**
- 546a02f, 8090d17 — schon bei Jan
- 8567ffa — upstream-Commits bereits direkt vorhanden

---

**Zusammenfassung (55 Wörter):** j3rm hat 4 echte neue Patches, alle im Watchdog/Resume-Picker-Bereich. Der kritischste Bug: Jans `start_session()` nutzt noch Escape+Ctrl-C um den --resume-Picker zu verlassen — das triggert einen Restart-Loop. Parallel fehlt der `clean_recent`-Guard im Watchdog (stale-scrollback-Loop). Commits 3+4+6+7 als Einheit cherry-picken, 1+2+5 überspringen.
